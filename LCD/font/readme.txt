Expected format is a directory, with a file for each character.
Each file is named with the ASCII code for the character, e.g. 'a' is 97.bin
The file represents a character with width and height 7 x 13, each byte is the alpha value for the pixel travesing along x then y